# Package manifest for host-testable whirl-rig-specific computation.

[package]
name = "whirl-rig-program"
description = "Hardware-independent programme and DSP components specific to the HELIC-DAQ whirl rig"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
rt-sram = []

[dependencies]
